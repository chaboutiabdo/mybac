# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local env
.env
.env.*
!.env.example

# written by scripts/stackhawk-spec.mjs before each API scan
stackhawk-openapi.json
# local state of `wrangler pages dev` (the frontend scan serves dist/ with it)
.wrangler/

# the owner's local reports (the security audit describes live findings)
docs/
